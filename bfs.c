
#include "poodle.h"
struct weaknessCheck {
	int sourceComputer;
	int poodleCount;
};

// INCOMPLETE
// TASKS
// - implement a queue structure to implement this function's
//   underlying search algorithm
// - implement this function via  the BFS algorithm
struct weaknessCheck
comHackImpact(int srcComputer, struct computer coms[], int numComputers,
              struct connection conns[] int numConnections) {
	//scaffold
	struct weaknessCheck res = {0, 0};
	return res;
}
